{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Endereco</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ entity.nome }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ entity.numero }}</td>
            </tr>
            <tr>
                <th>Cep</th>
                <td>{{ entity.cep }}</td>
            </tr>
        </tbody>
    </table>

        <a class="btn btn-default go-back" href="{{ path('cms_endereco') }}">
    Voltar
</a>

    <li>
        <a  href="{{ path('cms_endereco_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
{% endblock %}
