{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Banner</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ entity.title }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ entity.text }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ entity.image }}</td>
            </tr>
            <tr>
                <th>Linkdescription</th>
                <td>{{ entity.linkDescription }}</td>
            </tr>
            <tr>
                <th>Linktarget</th>
                <td>{{ entity.linkTarget }}</td>
            </tr>
        </tbody>
    </table>

        <a class="btn btn-default go-back" href="{{ path('cms_banner') }}">
    Voltar
</a>

    <li>
        <a  href="{{ path('cms_banner__edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
{% endblock %}
