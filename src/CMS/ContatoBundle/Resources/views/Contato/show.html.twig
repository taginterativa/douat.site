{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Contato</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ entity.nome }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ entity.email }}</td>
            </tr>
            <tr>
                <th>Descricao</th>
                <td>{{ entity.descricao }}</td>
            </tr>
        </tbody>
    </table>

        <a class="btn btn-default go-back" href="{{ path('cms_contato') }}">
    Voltar
</a>

    <li>
        <a  href="{{ path('cms_contato_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
{% endblock %}
